# Example known population data

Example of a household survey dataset, used in unit tests and vignettes
for the `networkreporting` package.

## Usage

``` r
example.knownpop.dat
```

## Format

A data frame with 22 rows and 2 variables:

- known.popn:

  The name of the group

- size:

  The number of people in the group
