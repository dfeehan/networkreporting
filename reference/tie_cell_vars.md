# Which columns define a cell, given a set of estimate tables?

Internal. The cell columns are whatever the tables share that is not an
estimated quantity.

## Usage

``` r
tie_cell_vars(tabs)
```

## Arguments

- tabs:

  a list of estimate tables

## Value

character vector of cell column names
