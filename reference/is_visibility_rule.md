# Is this object a visibility rule?

Is this object a visibility rule?

## Usage

``` r
is_visibility_rule(x)
```

## Arguments

- x:

  object to test

## Value

`TRUE` if `x` is a `visibility_rule`
