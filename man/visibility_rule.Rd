% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility_rules.R
\name{visibility_rule}
\alias{visibility_rule}
\title{Construct a visibility rule}
\usage{
visibility_rule(
  label,
  requires,
  is_estimated,
  fit,
  predict,
  assumptions = character(0),
  params = list(),
  applies_to = NA_character_,
  tie_overridable = character(0),
  declared = list(),
  assumptions_fn = NULL
)
}
\arguments{
\item{label}{short string naming the rule; appears in provenance output}

\item{requires}{character vector of columns the rule needs, checked up front}

\item{is_estimated}{\code{TRUE} if \code{fit()} consumes the sample. Drives the
bootstrap path: see \code{\link[=apply_visibility_rule]{apply_visibility_rule()}}}

\item{fit}{\verb{function(donor.dat, weights)} returning opaque state}

\item{predict}{\verb{function(alter.rows, state)} returning one row per row of
\code{alter.rows}, with columns \code{vis}, \code{vis_weight} and \code{vis_rule}}

\item{assumptions}{character vector of assumptions this rule makes, carried
into the provenance table so they reach output rather than living
only in a methods appendix}

\item{params}{list of the constructor's arguments, for printing}
}
\value{
an object of class \code{visibility_rule}
}
\description{
A rule is a closure, not a table, and it is split into \code{fit} and \code{predict}.
The split is what lets one interface cover exact derivation, donor means and
(later) a fitted model, and what makes correct bootstrapping possible: a rule
whose \code{fit} step consumes the sample must be refit inside each bootstrap
replicate, and one whose does not must not be.
}
\keyword{internal}
