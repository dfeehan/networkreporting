% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility_rules.R
\name{vis_from_donor}
\alias{vis_from_donor}
\title{Visibility borrowed from a donor population}
\usage{
vis_from_donor(
  donor = "egos",
  match_on = NULL,
  statistic = c("harmonic", "arithmetic", "median"),
  donor_vis = vis_from_clique(),
  min_donors = 25,
  on_missing = c("error", "fallback", "na")
)
}
\arguments{
\item{donor}{\code{"egos"} to use the survey respondents, or a data frame of
donors supplied directly}

\item{match_on}{covariates to match alters to donors on, or \code{NULL} for one
global value. Names the alter's columns; where the donor frame spells
a covariate differently, use a named vector, as in
\code{c(.sib.sex = "sex")}}

\item{statistic}{\code{"harmonic"} (the default, and the right one for the
individual estimator), \code{"arithmetic"} or \code{"median"}}

\item{donor_vis}{how the donors' \emph{own} visibility is derived; a
\link{visibility_rule}, defaulting to \code{\link[=vis_from_clique]{vis_from_clique()}}}

\item{min_donors}{cells with fewer donors than this are treated as having no
donors at all}

\item{on_missing}{what to do about an alter whose donor cell is missing or
too small: \code{"error"}, \code{"fallback"} (use the global value) or \code{"na"}}
}
\value{
a \link{visibility_rule}
}
\description{
The approximation, and the first \code{is_estimated = TRUE} rule. Where
visibility cannot be derived from ego's own reports -- which is the normal
case for any tie that is not a clique -- borrow it from a donor population,
optionally matched on covariates.
}
\details{
Read it as the simplest member of the predict-from-data family: \code{fit()} is a
grouped weighted mean where a model's would be a regression.
}
\section{What this assumes, and which way it is wrong}{


Donors are respondents: alive, and on the frame. A large share of the alters
needing an imputed visibility are dead. Wherever visibility correlates with
mortality -- through family size for kin ties, through living arrangements
for household ties -- the donor is systematically wrong, not merely noisy.
The direction is recorded in the rule's assumptions so that it reaches the
provenance table rather than staying in a methods appendix.
}

\section{Harmonic or arithmetic}{


The default is the weighted harmonic mean, because the individual estimator
averages \code{1/v}: the functional that makes the plug-in unbiased is
\code{(E[1/v])^-1}, not \code{E[v]}. \code{"arithmetic"} remains available, since it is
what the historical \code{y.F.bar / (y.F.bar + 1)} adjustment factor used. By
Jensen's inequality harmonic <= arithmetic, so the two disagree in a known
direction, by an amount that grows with the variance of visibility.
}

\section{Coverage failure is routine}{


\code{match_on} describes the alter, but donors are respondents. DHS interviews
women aged 15-49, so an alter aged 60 has no donor cell at all. Left alone
that surfaces as \code{NA} propagating silently into rates; \code{min_donors} together
with \code{on_missing = "error"} makes it loud instead. Use \code{\link[=vis_coalesce]{vis_coalesce()}} to
fall back to a coarser rule rather than to \code{NA}.
}

\examples{
vis_from_donor(match_on = c(.sib.sex = "sex"))
}
