% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/true_visibility.R
\name{visibility_accuracy}
\alias{visibility_accuracy}
\title{Score a visibility rule against a known truth}
\usage{
visibility_accuracy(predicted, truth, in.frame)
}
\arguments{
\item{predicted}{numeric vector of predicted visibilities, one per report}

\item{truth}{numeric vector of true visibilities, the same length}

\item{in.frame}{logical or 0/1 vector saying whether each \strong{alter} is in
the frame population. Note this is the alter's status --- unlike
\code{\link[=true_visibility_from_network]{true_visibility_from_network()}}, where it is the reporter's}
}
\value{
a \code{visibility_accuracy} object: per-side counts, the share predicted
exactly, the mean ratio of predicted to true, and the differential
}
\description{
Compares predicted visibilities to true ones, \strong{separately for alters in and
out of the frame population}, and reports the ratio between those two
errors.
}
\section{Why the split, and why the ratio}{


Visibility reaches a death rate only through the asymmetry between on-frame
and off-frame alters: every death is off-frame, while exposure is a mixture.
An error of the same size on both sides therefore largely cancels out of the
rate. A \emph{differential} one does not --- it biases it.

So an overall accuracy figure is close to useless here. A rule can be
badly wrong on both sides and still give an almost unbiased rate, or mildly
wrong in a lopsided way and bias it substantially. \code{differential} is the
number to read.
}

\examples{
visibility_accuracy(predicted = c(3, 4, 3, 4),
                    truth     = c(3, 3, 3, 4),
                    in.frame  = c(TRUE, FALSE, TRUE, FALSE))
}
\seealso{
\code{\link[=true_visibility_from_network]{true_visibility_from_network()}}
}
