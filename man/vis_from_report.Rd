% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility_rules.R
\name{vis_from_report}
\alias{vis_from_report}
\title{Visibility as reported by the respondent}
\usage{
vis_from_report(
  report.var,
  counts.ego = TRUE,
  counts.self = FALSE,
  on_missing = c("error", "na"),
  on_impossible = c("error", "floor", "na"),
  label = NULL
)
}
\arguments{
\item{report.var}{name of the column holding ego's reported count for each
alter}

\item{counts.ego}{did the respondent count themselves? When \code{FALSE}, one is
added back}

\item{counts.self}{did the answer count the alter themselves? When \code{TRUE},
one is subtracted for an alter who is in the frame population, since
an alter cannot report themselves}

\item{on_missing}{what to do about an alter with no reported value:
\code{"error"} or \code{"na"} (leave unresolved, so \code{\link[=vis_coalesce]{vis_coalesce()}} can try the
next tier)}

\item{on_impossible}{what to do about a reported visibility below one:
\code{"error"}, \code{"floor"} (raise it to one) or \code{"na"}. An alter who was
reported was, by construction, visible to at least one person, so a
zero is a data problem rather than a small number}

\item{label}{optional short name for provenance}
}
\value{
a \link{visibility_rule}
}
\description{
Ego is asked, about each alter, how many frame-population members that alter
is connected to. The answer is the alter's visibility, read straight off the
questionnaire instead of derived from a roster or borrowed from donors.
}
\section{Why this is the honest option for some ties}{


Every other rule here recovers visibility from something the survey happened
to collect for another purpose. \code{\link[=vis_from_clique]{vis_from_clique()}} exploits the tie's
structure, \code{\link[=vis_from_donor]{vis_from_donor()}} and \code{\link[=vis_from_model]{vis_from_model()}} substitute the
respondents' own. All three are attempts to get at a quantity nobody was
asked about.

For an \code{"unbounded"} tie there is no way round that. Neighbours and
acquaintances have no bounded group to count, so there is no roster to
derive from and nothing that makes the respondents a good stand-in for the
alters. Asking is the only route to the quantity, and this rule is what
turns the answer into an estimate.

It is a \strong{survey-design choice} as much as an analysis one: it costs
questionnaire time, and it can only be used if somebody decided in advance
to ask. Where the question was asked, though, it beats an approximation
derived from something else --- and unlike the other rules, it puts the
uncertainty somewhere visible, in reporting error rather than in an
assumption.
}

\section{Two things the answer may or may not include}{


The wording of the question decides both, and the package cannot tell from
the numbers which was meant.
\itemize{
\item \code{counts.ego} --- did the respondent count \emph{themselves} among the alter's
connections? "How many people like you does X know?" usually includes
them; "how many \emph{other} people like you" does not, and then one has to be
added back.
\item \code{counts.self} --- did the answer count the alter? A question phrased about
a \emph{group} ("how many people are in X's household?") does; one phrased about
connections does not. When it does, this rule behaves like
\code{\link[=vis_from_group_size]{vis_from_group_size()}}, which is the better constructor to reach for.
}
}

\examples{
vis_from_report("n_known_by")
vis_from_report("n_other_known_by", counts.ego = FALSE)
}
\seealso{
\code{\link[=vis_from_group_size]{vis_from_group_size()}}, for a reported \emph{group size} rather than a
reported degree
}
