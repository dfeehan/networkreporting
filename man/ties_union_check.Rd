% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_ties.R
\name{ties_union_check}
\alias{ties_union_check}
\title{Can these ties be unioned?}
\usage{
ties_union_check(..., alter.key = NULL)
}
\arguments{
\item{...}{report-level data frames, named by tie. Usually the \code{esc.dat}
from each \code{\link[=network_survival_estimator]{network_survival_estimator()}} result}

\item{alter.key}{name of a column identifying an alter \emph{across} ties}
}
\value{
a \code{ties_union_check}, invisibly; printed for its report
}
\description{
Union is the third combination: treat several ties as one larger tie and
estimate once from their pooled reports. Unlike \code{\link[=compare_ties]{compare_ties()}} and
\code{\link[=pool_ties]{pool_ties()}} it cannot be done from finished estimates, because it changes
what is estimated rather than combining answers --- the reports have to be
pooled and the visibility recomputed on the union.
}
\details{
It also needs something the package does not carry by default: \strong{an identity
for alters that holds across ties.} An alter reachable through two ties must
be recognised as one alter, or their reports are counted twice and their
visibility computed as though two separate people. Alter ids in this package
are unique only within an ego, which is not enough.

This function checks whether a supplied key does the job, and reports what
union would involve. It does not perform the union: once the key exists, the
operation is to bind the report rows, deduplicate on the key, and estimate
once with a \code{tie_config()} describing the union --- which is usually \emph{not}
the structure of either part. Two cliques unioned are generally not a clique.
}
\examples{
\dontrun{
ties_union_check(maternal = a$esc.dat, paternal = b$esc.dat,
                 alter.key = "person_id")
}
}
\seealso{
\code{\link[=pool_ties]{pool_ties()}}, which averages estimates instead and needs no such key
}
