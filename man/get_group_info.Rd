% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility_rules.R
\name{get_group_info}
\alias{get_group_info}
\title{Count group members on the frame, for each ego}
\usage{
get_group_info(dat, ego.id, frame.indicator, ego.in.group = TRUE)
}
\arguments{
\item{dat}{long-form alter data, one row per ego X alter}

\item{ego.id}{name of the column holding the ego id}

\item{frame.indicator}{name of the 0/1 column saying whether each alter is
in the frame population}

\item{ego.in.group}{is ego a member of the group being reported about?}
}
\value{
a tibble with one row per ego and columns \code{y.F} (alters on frame,
not counting ego), \code{yprime.F} (on-frame group members including ego
when \code{ego.in.group}) and \code{sib.size} (group size)
}
\description{
The generic form of \code{get_sibship_info()}. Counts, for each ego, how many of
the alters ego reports are in the frame population, and adds one for ego
when ego is a member of the group ego reports about.
}
\details{
That \code{+ 1} is the whole content of \code{ego.in.group}. It is not about siblings:
it encodes "ego belongs to the group ego reports about", which is true of
siblings and of households, and false of parents and of neighbours.
}
\keyword{internal}
