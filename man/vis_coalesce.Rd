% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility_rules.R
\name{vis_coalesce}
\alias{vis_coalesce}
\title{Try visibility rules in priority order}
\usage{
vis_coalesce(...)
}
\arguments{
\item{...}{two or more \link{visibility_rule} objects, most preferred first}
}
\value{
a \link{visibility_rule}
}
\description{
Takes rules in priority order. For each row, the first rule returning a
non-\code{NA} visibility wins, and the row records which tier resolved it.
}
\details{
This is the mixed case -- some alters derivable exactly, some not -- and it
is what turns "38\% of reported cousins were approximated, 4\% of them from the
global mean" into a number the package reports rather than an assumption
nobody wrote down.

Rules that would otherwise \code{stop()} on an unresolved row are run as though
\code{on_missing = "na"}, since falling through is the entire point here.
}
\examples{
vis_coalesce(vis_from_clique(),
             vis_from_donor(match_on = c(.sib.sex = "sex")),
             vis_from_donor(match_on = NULL))
}
