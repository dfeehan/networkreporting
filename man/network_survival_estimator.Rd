% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_survival_estimator.R
\name{network_survival_estimator}
\alias{network_survival_estimator}
\title{Estimate death rates from network reporting data}
\usage{
network_survival_estimator(
  rel.dat,
  ego.id,
  alter.id,
  frame.indicator,
  alter.sex = "sex",
  cell.config,
  weights,
  boot.weights = NULL,
  return.boot = FALSE,
  visibility = vis_from_clique(),
  tie,
  discretize.exp = FALSE,
  .arg.labels = character(0),
  .data.label = "rel.dat"
)
}
\arguments{
\item{rel.dat}{The long-form ego X alter dataset: one row per reported alter,
per ego}

\item{ego.id}{String naming the column of \code{rel.dat} with the survey
respondent's id}

\item{alter.id}{String naming the column of \code{rel.dat} with the alter's id}

\item{frame.indicator}{String naming the 0/1 column of \code{rel.dat} saying
whether each alter is in the frame population}

\item{alter.sex}{String naming the alter attribute that enters the estimation
cells alongside age and time period. Called \code{alter.sex} because sex is
what it is in every current application; carrying several such
attributes, rather than one plus \code{cell.config$covars}, is future
work}

\item{cell.config}{An object from \code{\link[=cell_config]{cell_config()}} configuring the cells}

\item{weights}{String naming the column of \code{rel.dat} with the sampling
weight}

\item{boot.weights}{Optional dataframe of bootstrap resampled weights; see
Details}

\item{return.boot}{If TRUE, and \code{boot.weights} is given, return every
bootstrap estimate rather than only their summaries}

\item{visibility}{A \link{visibility_rule} saying how each alter's visibility is
derived. Defaults to \code{\link[=vis_from_clique]{vis_from_clique()}}, which is exact for a clique
tie and refuses any other structure}

\item{tie}{A \code{\link[=tie_config]{tie_config()}} saying what kind of tie these reports are about.
\strong{Required}; see above}

\item{discretize.exp}{Boolean for whether exposure should be discretized. Not
yet implemented}

\item{.arg.labels}{Internal. Named character vector letting a wrapper phrase
the up-front column-check message in its own argument names}

\item{.data.label}{Internal. Name to use for the data argument in that message}
}
\value{
a list with \code{asdr.ind} (individual-visibility estimates),
\code{asdr.agg} (aggregate-visibility estimates), \code{ec.dat},
\code{esc.dat}, and a \code{vis_provenance} object saying how
visibility was arrived at
}
\description{
The generic network survival estimator. Given reports about alters connected
to survey respondents by some tie, it produces age-specific rates using both
the individual-visibility and the aggregate-visibility estimators.
}
\details{
\code{\link[siblingsurvival:sibling_estimator]{siblingsurvival::sibling_estimator()}} is this function with the sibling
names and the clique tie filled in; if you are working with sibling
histories, use that.
}
\section{The tie is required}{


There is no default \code{tie}, and that is the point. Which kind of tie a set of
reports is about cannot be read off the data: on a tie that is not a clique,
the default \code{\link[=vis_from_clique]{vis_from_clique()}} rule still returns a finite, plausible
number, and it can be wrong. Measured against socsim ground truth on a
roster that pools maternal and paternal cousins --- which is not a clique,
even though each line separately is --- it overstates visibility by 1.089x
for off-frame alters against 1.061x for on-frame ones. Because a death is
always off-frame while exposure is a mixture, that differential biases the
rate rather than cancelling out of it. See \code{\link[=tie_config]{tie_config()}}.
}

\section{Details}{

If you want estimated sampling variances, pass a data frame
\code{boot.weights}. It is assumed to have a column named whatever
\code{ego.id} is, and then columns \code{boot_weight_1}, ...,
\code{boot_weight_M}.
}

\seealso{
\code{\link[siblingsurvival:sibling_estimator]{siblingsurvival::sibling_estimator()}}, \code{\link[=tie_config]{tie_config()}},
\code{\link[=vis_from_clique]{vis_from_clique()}}, \code{\link[=vis_from_donor]{vis_from_donor()}}
}
