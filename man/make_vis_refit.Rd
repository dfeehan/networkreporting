% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility_rules.R
\name{make_vis_refit}
\alias{make_vis_refit}
\title{Build a per-replicate refit function for an estimated visibility rule}
\usage{
make_vis_refit(rule, donor.dat, boot.weights, ec.dat, ego.id = ".ego.id")
}
\arguments{
\item{rule}{the \link{visibility_rule}}

\item{donor.dat}{the donor frame}

\item{boot.weights}{data frame of bootstrap weights, with an ego id column
and columns \code{boot_weight_1} ... \code{boot_weight_M}}

\item{ec.dat}{the ego X cell data the estimate is computed from}

\item{ego.id}{name of the ego id column}
}
\value{
\verb{function(r)} returning a numeric vector, one \code{S.hat} per row of
\code{ec.dat}, or \code{NULL} if the rule does not estimate from the sample
}
\description{
For a rule with \code{is_estimated = TRUE}, the estimated group size moves with
the bootstrap replicate. This returns the \code{refit} closure
\code{\link[=get_boot_ests_matrix]{get_boot_ests_matrix()}} expects: given a replicate index, it refits the
rule using that replicate's weights and returns the group size \code{S.hat} for
each row of \code{ec.dat}.
}
\details{
Predicting with the frame indicator set to zero returns \code{S.hat} itself,
since a rule's visibility for an off-frame alter \emph{is} the group size.
}
