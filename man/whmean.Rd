% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility_rules.R
\name{whmean}
\alias{whmean}
\title{Weighted harmonic mean}
\usage{
whmean(x, w)
}
\arguments{
\item{x}{numeric vector; must be strictly positive}

\item{w}{numeric vector of weights, the same length as \code{x}}
}
\value{
the weighted harmonic mean, \code{sum(w) / sum(w/x)}
}
\description{
The individual estimator averages \code{1/v}, so the functional that makes a
plug-in visibility unbiased is \code{(E[1/v])^-1}, not \code{E[v]}. This is the
default summary for \code{\link[=vis_from_donor]{vis_from_donor()}}.
}
\details{
Restored from \code{get_visibility()}, which carried it as \code{wh.mean()} until the
ad-hoc adjustment factors were removed.
}
\keyword{internal}
