% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility_rules.R
\name{make_vis_refit_esc}
\alias{make_vis_refit_esc}
\title{Build a per-replicate refit that recomputes visibility at the report level}
\usage{
make_vis_refit_esc(
  rule,
  donor.dat,
  boot.weights,
  esc.dat,
  ec.dat,
  cell.vars,
  ego.id = ".ego.id"
)
}
\arguments{
\item{rule}{the \link{visibility_rule}}

\item{donor.dat}{the donor frame}

\item{boot.weights}{data frame of replicate weights}

\item{esc.dat}{the ego X alter X cell reports, carrying \code{ind_vis}'s inputs}

\item{ec.dat}{the ego X cell data the estimate is computed from}

\item{cell.vars}{the columns defining a cell}

\item{ego.id}{name of the ego id column}
}
\value{
\verb{function(r)} returning a two-column data frame of \code{num} and \code{denom},
one row per row of \code{ec.dat}, or \code{NULL} if the rule is not estimated
}
\description{
Internal. The expensive bootstrap path, for an estimated rule whose
visibility is \emph{not} constant within an ego X cell row. There the frame-split
identity does not apply, and the only correct route is to refit the rule and
re-predict for every report inside each replicate, then re-aggregate.
}
\details{
Costs roughly M times a point estimate. That is the price of not freezing a
sample quantity; falling back to the cheap path here would silently reinstate
the very bug the refit exists to prevent.
}
