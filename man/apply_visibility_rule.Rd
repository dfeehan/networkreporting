% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility_rules.R
\name{apply_visibility_rule}
\alias{apply_visibility_rule}
\title{Apply a visibility rule to ego X alter X cell reports}
\usage{
apply_visibility_rule(
  rule,
  esc.dat,
  ego.dat = NULL,
  sib.dat = NULL,
  ego.id = ".ego.id",
  frame.indicator = ".sib.in.F",
  weights = NULL,
  ego.in.group = TRUE
)
}
\arguments{
\item{rule}{a \link{visibility_rule}}

\item{esc.dat}{ego X alter X cell reports, one row per report}

\item{ego.dat}{ego-level data, used as the donor frame when a rule asks for
\code{donor = "egos"}. Needs a \code{y.F} column; if it has none and \code{sib.dat}
is supplied, one is derived}

\item{sib.dat}{long-form alter data, used to derive \code{y.F} when \code{esc.dat} or
\code{ego.dat} lacks it}

\item{ego.id}{name of the ego id column}

\item{frame.indicator}{name of the 0/1 frame membership column}

\item{weights}{name of the column holding donor sampling weights}

\item{ego.in.group}{passed through when deriving \code{y.F}}
}
\value{
a list with \code{values} (a tibble of \code{vis}, \code{vis_weight}, \code{vis_rule},
one row per row of \code{esc.dat}) and \code{provenance} (a \code{vis_provenance}
tibble)
}
\description{
Validates the rule's \code{requires} against the columns actually present, fits
the rule, predicts a visibility for every row, and returns both the values
and a provenance table describing how they were arrived at.
}
